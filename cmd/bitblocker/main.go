// Command bitblocker is the BitBlocker daemon entry point.
//
// It wires configuration, logging, the blocklist cache, the data fetcher,
// and the forwardAuth HTTP server. See docs/BitBlocker.md for the product
// overview and docs/bitblocker-spec.md for the component specification.
package main

import "fmt"

func main() {
	fmt.Println("bitblocker: scaffolding only; no components wired yet")
}
