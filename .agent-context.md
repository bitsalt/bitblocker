# bitblocker — Agent Context

> **For agents only.** Target: scannable in under a minute (~100 lines / under 3 KB as a sanity check). Pointers and non-obvious invariants only — anything paraphrasing a doc belongs as a link instead.

---

## One-liner

Self-hosted single-binary Go daemon that drops inbound traffic from selected countries at the Traefik edge via a `forwardAuth` middleware, for operators running small-scale self-hosted infrastructure.

## Stack

- **Language:** Go 1.22.2 (toolchain pinned in `go.mod`).
- **Framework:** standard library + `oschwald/maxminddb-golang v1.13.1` (MMDB reader, pinned below the Go 1.24 floor v2 introduced); `maxmind/mmdbwriter v1.0.0` (test-only); `gopkg.in/yaml.v3`; `stretchr/testify`.
- **Tests:** `go test -race ./...`.
- **Build / package manager:** Go modules + `Makefile` (wraps `go build`, `go test`, `golangci-lint run`, `govulncheck`).
- **Deploy:** none yet — Sprint 4 ships multi-stage Dockerfile, systemd unit, and a GHA release workflow producing linux/amd64 + linux/arm64 binaries. No deploy substrate exists today.

## Stack addenda to read

In addition to `coding-standards/coding-standards.md`:
- `coding-standards/coding-standards-go.md`

## Key file locations

- **Sprint file:** [`docs/BitBlocker.md`](docs/BitBlocker.md) — milestones, sprint tasks, decisions log, Open Questions, Carry-over.
- **Product spec:** [`docs/bitblocker-spec.md`](docs/bitblocker-spec.md) — full design, data sources, HTTP API, Traefik integration, phased roadmap.
- **Entry point:** `cmd/bitblocker/main.go`.
- **Internal packages:** `internal/{blocklist,config,fetcher,logging,mmdb,server}/`.
- **Tests:** colocated `_test.go` alongside each package.
- **CI:** `.github/workflows/ci.yml`.
- **Pre-commit hook:** `scripts/git-hooks/pre-commit` (raw shell; activate via `git config core.hooksPath scripts/git-hooks`).
- **Config template:** `config.example.yaml`.

## Current sprint goal

Sprint 2 (May 5 → May 18): daemon answers `/check` and `/healthz` correctly against a preloaded MMDB fixture. **All three slices landed:** MMDB loader (PR #2) ✅, the request-path slice (PR #5 — `/check` + `X-Real-IP`/rightmost-XFF extraction + `/healthz` + fail-closed + handler tests) ✅, and the swap+disk-cache slice (PR #8 ADRs + PR #9 implementation — `blocklist.Source` backed by `atomic.Pointer[blocklist.Trie]` + `internal/diskcache` snapshot read/write) ✅. Sprint goal **met**: the daemon consults a populated trie via the swap source, with a disk-cache cold-start fast-path.

## Active blockers / live Open Questions

Synced from [`docs/BitBlocker.md`](docs/BitBlocker.md) § Open Questions — read there for full context:

- **MaxMind license key procurement** (Jeff) — blocks Sprint 3 fetcher work end-to-end.
- **MMDB country match scope** (Jeff) — `country.iso_code` only vs. also `registered_country.iso_code`. v1 currently matches `country` only; decide before v1 release.
- **Toolchain bump to Go 1.24** (Jeff) — would unblock `maxminddb-golang/v2` and recent `mmdbwriter`. Current pin works for v1.0; separate sprint-level decision.
- **`govulncheck` stdlib findings on `go 1.22.2`** (DevOps) — originally `GO-2025-3750` (Windows-only `os@go1.22.2`); refreshed 2026-05-08 (PR #5) to note eight stdlib findings now visible in CI. Pre-existing on `main`; surfaced now because PR #5 is the first PR to make the vuln-scan job meaningful. Specific CVE IDs to reconcile at next DevOps pass; toolchain bump above closes stdlib-rooted findings.
- **`golangci-lint` GitHub Action version** (DevOps) — workflow uses action `@v6` against pinned linter `v2.11.4`; needs `@v7` for v2-line compatibility. Pre-existing on `main`; surfaced 2026-05-08 (PR #5).
- **Deferred features** (Jeff) — ASN blocking via BGP.tools (v1 → v2?); allowlist for admin/monitoring IPs; leftmost-XFF config knob for upstream-CDN scenarios.

## Key invariants

- **Fail-closed posture is load-bearing.** Cold start with no ruleset default-denies. Disk cache, `behavior.startup_mode: fail-closed | fail-open` knob, `/healthz` 503 while empty, and a bounded retry budget make it operationally tolerable. See decisions log 2026-04-22.
- **Header selection is `X-Real-IP` first, then rightmost-XFF.** Leftmost-XFF is spoofable under Traefik's `trustForwardHeader: true`. Malformed `/check` fails closed. Decisions log 2026-04-22.
- **Atomic swap only.** Blocklist updates are a lock-free pointer-swap via `atomic.Pointer[blocklist.Trie]`; partial or corrupted updates never reach lookup. RWMutex was considered and rejected — see `docs/adr/0001-blocklist-swap-via-atomic-pointer.md`. Spec § Blocklist update strategy.
- **MMDB binary, not CSV.** MaxMind data is consumed via `maxminddb-golang`. Decisions log 2026-04-22.
- **IPv4 + IPv6 are first-class** (against the original spec's deferred-IPv6 stance). The trie has separate v4/v6 roots on `net/netip`; `Contains` normalizes IPv4-in-IPv6 via `Unmap`.
- **MaxMind license key may come from `MAXMIND_LICENSE_KEY` env or inline YAML; env wins.** Prefer env so the secret does not sit on disk.
- **Toolchain pin is deliberate.** Do not bump `go.mod`'s `go 1.22.2` or the MMDB library versions without resolving the toolchain-bump Open Question first — `maxminddb-golang/v2` introduced a Go 1.24 floor.
- **Repo classification: production-touching.** All changes go via feature branch + PR per `vault-CLAUDE.md`. `gh pr create` after push.
- **License: MIT** (decisions log 2026-04-23). Relicensing is possible later if the project heads toward a CNCF/Apache-umbrella home.
- **`golangci-lint` pinned to v2.11.4 in CI**; local installs run whatever `go install @latest` resolves. If local and CI diverge, bump the CI pin.
- **Pre-commit is raw shell, not the `pre-commit` framework.** Solo-Go project; no Python toolchain pulled in for one linter.

## Status

- **Phase:** Sprint 2 in flight.
- **Stability:** Sprint 1 foundation in place (config, logging, CIDR trie with benchmarks comfortably under the 1ms/check spec budget); MMDB loader landed (PR #2); request-path slice landed (PR #5 — `/check` + IP extraction + `/healthz` + fail-closed + handler tests); swap+disk-cache slice landed (PR #9 — `blocklist.Source` over `atomic.Pointer`, `internal/diskcache` snapshot read/write, `cache.path`/`cache.max_age` config). Daemon answers `/check` and `/healthz` end-to-end and consults a populated trie via the swap source. Still missing: fetcher, scheduler, deploy artifacts.
- **Next milestones:** Sprint 3 ships fetcher + scheduler + cold-start retry; Sprint 4 ships v1.0 (binaries + Docker + operator docs); Sprint 5 ships v1.1 observability (`/metrics`, CLI subcommands, alert webhook).

## Notes specific to this project

- The product spec at `docs/bitblocker-spec.md` lists IPv6 as deferred and ASN blocking as an in-scope v1 feature. **The sprint file supersedes the spec on both points** — IPv6 was promoted to Sprint 1 and ASN-via-BGP.tools was deferred from v1 (decisions log 2026-04-22). When the two disagree, the decisions log wins.
- Lessons written from this project live under `~/projects/agent-knowledge-base/lessons/maxminddb/` (e.g., `version-floors-and-aliasing-gotchas.md` from PR #2). Read before touching the MMDB layer.
